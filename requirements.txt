pandas>=2.2.0,<4  # future.no_silent_downcasting 选项需要 2.2+
numpy>=1.24.0,<3
openpyxl>=3.1.0
xlrd>=2.0.0
Pillow>=10.0.0
PyQt6>=6.6.0,<7  # PyQt 界面（smart_table_quick_analysing_hub.py）
python-calamine>=0.2.0  # 快速 Excel 读取引擎（大文件提速数倍，缺失时自动回退 openpyxl）
certifi>=2024.2.2  # 打包后应用的 HTTPS CA 证书（应用内检查更新用）
lxml>=5.0.0  # openpyxl 写 xlsx 的加速路径（大文件保存提速约 2-3 倍）
matplotlib>=3.8.0  # Python 分析的图表预设（直方图/条形图/散点图/箱线图）
pyobjc-framework-Quartz>=10.0; sys_platform == "darwin"  # 图片队列"自动模式"全局 Cmd+V 监听（缺失时复选框隐藏）
anthropic>=1.0.0  # Python 分析窗口的「AI 生成代码」（Claude）；缺失时该菜单会提示安装
keyring>=24.0.0  # 可选：AI 生成代码的 API Key 存入系统钥匙串（macOS Keychain / Windows 凭据管理器）；缺失时回退 QSettings
